Below is the JSON payload containing the analyzed sales and inventory data for this week. It includes total sales, projected demand (accounting for MoM growth), stock risks, and mathematically calculated reorder quantities.

Write the briefing by completing each section below IN ORDER.

---

### SECTION 1: Executive Summary
Write a brief executive summary of the past month's performance.

---

### SECTION 2: Top Performers
Identify the top 2-3 selling SKUs by Revenue_M4.
RULE: For EACH SKU you mention, state its MoM_Growth_Avg as a percentage (e.g., "growing at 8.3% MoM").
RULE: Also mention their Total_M4 volume and Revenue_M4 value.

---

### SECTION 3: Poor Performers / Dead Stock

FIRST: Check the `poor_performers` list in the data payload.

IF the `poor_performers` list is EMPTY (it contains zero items): Write "No SKUs currently meet our dead stock criteria (negative MoM growth with excess inventory above 6 months of cover). All products are either growing or have appropriate stock levels." Then move on to Section 4. Do NOT pick a SKU from elsewhere in the data. Do NOT invent a poor performer.

IF the `poor_performers` list is NOT empty: Identify the worst-performing SKU from THAT LIST ONLY and recommend a discount or bundling strategy to free up working capital. State the chosen SKU's MoM_Growth_Avg percentage and Effective_Months_Cover.

*** FORBIDDEN — READ CAREFULLY ***
Do NOT mention any "BioSynergy" SKU in this section. BioSynergy products are NEW Q1 2026 launches — they are NOT dead stock and NOT poor performers. Do NOT label any SKU as dead stock unless it appears in the `poor_performers` list.

---

### SECTION 4: Red Flags
List ALL SKUs where Is_At_Risk == True (i.e., every SKU in the `skus_at_risk` list from the payload). Present them in a table with their Current_Months_Cover, Effective_Months_Cover, and Target_Months_Cover.

RULE: Include EVERY SKU from the `skus_at_risk` list. Do not omit any.
RULE: Do NOT include any SKU where Is_At_Risk == False.
RULE: Remind the team that projections are based on trailing 4-month momentum and to consider upcoming seasonality factors.

---

### SECTION 5: Reorder Recommendations
Make reorder recommendations for at least 3 SKUs. Use the Suggested_Reorder_Qty values from the data.

RULE: Write out the business reasoning for each (referencing lead times, current pipeline, and target cover).
RULE: Prioritise by revenue contribution (Revenue_M4) vs. lead time, assuming constrained cash flow.
RULE: SKUs with Order_Arrival_Months = 0 can be fulfilled immediately from domestic supply lines — highlight this as an advantage (no shipping delay).

---

### SECTION 6: BioSynergy Line
Acknowledge the BioSynergy products as new Q1 2026 launches. Explain that to avoid over-ordering on an initial launch spike, we have conservatively modelled their future demand using their current M4 baseline rather than compounding their initial MoM growth.

---

### SECTION 7: Strategic Priority (Air Freight)

Step 1: Look at the `skus_at_risk` list in the data payload.
Step 2: Find the SKU with the HIGHEST Revenue_M4 value in that list.
Step 3: That SKU is your air freight candidate. No other SKU is acceptable.

RULE: State the SKU's Revenue_M4 value and Effective_Months_Cover explicitly in your reasoning.
RULE: Justify why paying an air freight premium protects top-line revenue.

You MUST end this section with exactly one line formatted like this (replace the example SKU with your pick):

**AIR FREIGHT SKU: Premium Supplement Tier 3 500g**

CRITICAL FORMATTING RULES FOR THE LINE ABOVE:
- The line MUST begin with ** (two asterisks)
- The line MUST end with ** (two asterisks)
- Do NOT omit the asterisks. Do NOT split them. The entire line is bold.

---

DATA PAYLOAD:

{{ json_payload }}
