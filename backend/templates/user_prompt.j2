Below is the JSON payload containing the analyzed sales and inventory data for this week. It includes total sales, projected demand (accounting for MoM growth), stock risks, and mathematically calculated reorder quantities.

1. Write an Executive Summary of the past month's performance.
2. Highlight what sold well vs. what sold poorly. For EACH SKU you mention in this section (top sellers AND poor performers), you MUST explicitly state its MoM_Growth_Avg percentage from the data payload (e.g., "growing at 8.3% MoM" or "declining at -4.1% MoM"). Specifically, call out the worst-performing SKU (dead stock) and reason about whether we should implement a discount or bundling strategy to free up working capital. CRITICAL: You MUST ONLY select your dead stock / poor performer SKU from the `poor_performers` list in the data payload. These are SKUs with negative MoM growth AND excess inventory. Do NOT classify any SKU as a poor performer if it does not appear in that list. Do NOT classify any 'Bioactive Blend' SKUs as poor performers or dead stock, as they are newly launched Q1 2026 products with intentionally conservative projections.
3. Create a 'Red Flags' section for SKUs falling below target cover. CRITICAL: You MUST ONLY select SKUs that are explicitly flagged as `Is_At_Risk == True` in the data payload. Do NOT mention any SKU in this section if its `Effective_Months_Cover` is greater than or equal to its `Target_Months_Cover`. Note that projections are based on trailing 4-month momentum; explicitly remind the team to consider upcoming seasonality factors.
4. Make reorder recommendations for at least 3 SKUs. Use the 'Suggested_Reorder_Qty' provided, but write out the genuine business reasoning for *why* we are ordering that amount (e.g., referencing lead times, current pipeline, and target cover). If multiple items need reordering, prioritise them: reason about which one is the highest priority based on its revenue contribution (Revenue_M4) vs. its lead time, assuming a constrained cash-flow environment. Note: SKUs with `Order_Arrival_Months = 0` can be fulfilled immediately from existing domestic supply lines — highlight this as an advantage when recommending reorders for these items, as it means stock can arrive with no shipping delay.
5. Acknowledge the 'Bioactive Blend' line as new Q1 2026 products. Explain to the team that to avoid over-ordering on an initial launch spike, we have conservatively modelled their future demand using their current M4 baseline rather than compounding their initial MoM growth.
6. **Strategic Priority (Air Freight):** Look at the `skus_at_risk` list in the data payload. Sort them by `Revenue_M4` descending. The SKU with the HIGHEST `Revenue_M4` in that list is your air freight candidate — it is the most critical to protect because it generates the most revenue and is at risk of stocking out. CRITICAL: You MUST select the single highest-revenue at-risk SKU. Do NOT select a lower-revenue SKU. State its Revenue_M4 value and Effective_Months_Cover explicitly. Justify why paying an air freight premium is worth it to protect this top-line revenue.

   You MUST end this section with exactly this line on its own, with the entire line wrapped in double-asterisk bold markers:
   **AIR FREIGHT SKU: Manuka Honey MGO 514+ 500g**
   Replace the example SKU name with your actual recommendation. The line MUST start with ** and end with ** — do not omit the bold markers.

DATA PAYLOAD:

{{ json_payload }}